<UserControl
    x:Class="CorePlanetMusicPlayer6.Controls.DevControls.AlbumControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CorePlanetMusicPlayer6.Controls.DevControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:CorePlanetMusicPlayer.Models"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <CollectionViewSource x:Name="GroupedItemsViewSource" IsSourceGrouped="True"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ScrollViewer>
            <StackPanel>
                <TextBlock Name="NameTextBlock"/>
                <TextBox Header="简介" Name="DescriptionTextBox"/>
                <TextBlock Name="MusicCountTextBlock"/>
                <TextBlock Name="DiscCountTextBlock"/>
                <TextBlock Name="ArtistsTextBlock"/>
            </StackPanel>
        </ScrollViewer>
        <ListView Name="MusicListView" Grid.Row="1" ItemsSource="{Binding Source={StaticResource GroupedItemsViewSource}}">
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate x:DataType="models:Disc">
                            <TextBlock Text="{Binding Number}"/>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:IMusic">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding TrackNumber}" Margin="0,8,8,8" Grid.RowSpan="2"/>
                        <TextBlock Text="{Binding Title}" Grid.Column="1"/>
                        <TextBlock Text="{Binding Artist}" Grid.Row="1" Grid.Column="1"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
