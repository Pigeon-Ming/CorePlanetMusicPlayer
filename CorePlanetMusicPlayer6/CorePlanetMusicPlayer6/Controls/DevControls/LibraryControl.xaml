<UserControl
    x:Class="CorePlanetMusicPlayer6.Controls.DevControls.LibraryControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CorePlanetMusicPlayer6.Controls.DevControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton Label="读取文件夹列表" Icon="Refresh" Name="RefreshButton" Click="RefreshButton_Click"/>
            <AppBarButton Label="添加" Icon="Add" Name="AddButton" Click="AddButton_Click"/>
            <AppBarButton Label="移除" Icon="Remove" Name="RemoveButton" Click="RemoveButton_Click"/>
        </CommandBar>
        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Margin="8" Text="音乐库位置"/>
        <ListView Grid.Row="1" Name="FolderListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Token}"/>
                        <TextBlock Grid.Column="1" Margin="8,0,0,0" Text="{Binding StorageFolder.Path}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
