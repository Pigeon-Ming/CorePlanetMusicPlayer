<UserControl
    x:Class="CorePlanetMusicPlayer6.Controls.DevControls.GenresControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CorePlanetMusicPlayer6.Controls.DevControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:CorePlanetMusicPlayer.Models"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton Label="重新整理数据" Icon="Refresh" Name="RefreshButton"/>
        </CommandBar>
        <ListView Name="GenresListView" Grid.Row="1" SelectionChanged="GenresListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Genre">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Text="{Binding Id}" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Row="1">
                            <Run Text="{Binding Music.Count}"/>
                            <Run Text="首"/>
                        </TextBlock>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock Text="选中的流派" Grid.Row="2"/>
        <local:GenreControl Grid.Row="3" x:Name="GenreControl"/>
    </Grid>
</UserControl>
