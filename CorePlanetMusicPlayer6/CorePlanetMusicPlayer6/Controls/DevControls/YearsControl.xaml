<UserControl
    x:Class="CorePlanetMusicPlayer6.Controls.DevControls.YearsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CorePlanetMusicPlayer6.Controls.DevControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:CorePlanetMusicPlayer.Models"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton
                Name="RefreshButton"
                Icon="Refresh"
                Label="重新整理数据" />
        </CommandBar>
        <ListView
            Name="YearsListView"
            Grid.Row="1"
            SelectionChanged="YearsListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Year">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding ReleaseYear}" />
                        <TextBlock Grid.Row="1">
                            <Run Text="{Binding Music.Count}" />
                            <Run Text="首" />
                        </TextBlock>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock Grid.Row="2" Text="选中的年份" />
        <local:YearControl x:Name="YearControl" Grid.Row="3" />
    </Grid>
</UserControl>
