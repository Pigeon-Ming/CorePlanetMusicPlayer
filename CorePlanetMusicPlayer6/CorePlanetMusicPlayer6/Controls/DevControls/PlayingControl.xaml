<UserControl
    x:Class="CorePlanetMusicPlayer6.Controls.DevControls.PlayingControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CorePlanetMusicPlayer6.Controls.DevControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" MinWidth="200" MinHeight="200"/>
        <TextBlock Grid.Row="1" Name="TitleTextBlock"/>
        <TextBlock Grid.Row="2" Name="MessagesTextBlock"/>
        <Slider Grid.Row="3" Margin="8" Name="ProgressSlider" RightTapped="ProgressSlider_RightTapped" ValueChanged="ProgressSlider_ValueChanged"/>
        <Grid Grid.Row="4">
            <TextBlock Name="ProgressTextBlock"/>
            <TextBlock Name="DurationTextBlock" HorizontalAlignment="Right"/>
        </Grid>
        <Grid Grid.Row="5" Margin="0,0,0,0">
            <Grid.Resources>
                <Style TargetType="Button">
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Name="PlayModeButton" Click="PlayModeButton_Click">
                <FontIcon Glyph="&#xE8EE;" Name="PlayModeIcon"/>
            </Button>
            <Button Grid.Column="1" Name="PreviousButton" Click="PreviousButton_Click">
                <FontIcon Glyph="&#xE892;" />
            </Button>
            <Button Grid.Column="2" Name="PlayAndPauseButton" Click="PlayAndPauseButton_Click">
                <FontIcon Glyph="&#xE768;" Name="PlayAndPauseIcon"/>
            </Button>
            <Button Grid.Column="3" Name="NextButton" Click="NextButton_Click" RightTapped="NextButton_RightTapped">
                <FontIcon Glyph="&#xE893;" />
            </Button>
            <Button Grid.Column="4" Name="VolumeButton">
                <FontIcon Glyph="&#xE767;" Name="VolumeIcon"/>
            </Button>
        </Grid>
        
    </Grid>
</UserControl>
