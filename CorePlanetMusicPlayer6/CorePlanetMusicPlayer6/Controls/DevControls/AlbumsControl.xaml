<UserControl
    x:Class="CorePlanetMusicPlayer6.Controls.DevControls.AlbumsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CorePlanetMusicPlayer6.Controls.DevControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:CorePlanetMusicPlayer.Models"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton Label="重新整理数据" Icon="Refresh" Name="RefreshButton"/>
        </CommandBar>
        <ListView Name="AlbumsListView" Grid.Row="1" SelectionChanged="AlbumsListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Album">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Grid.Row="1" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}">
                            <Run Text="{Binding MusicCount}"/>
                            <Run>首</Run>
                        </TextBlock>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock Text="选中的专辑" Grid.Row="2"/>
        <local:AlbumControl Grid.Row="3" x:Name="AlbumControl"/>
    </Grid>
</UserControl>
